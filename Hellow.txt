/* Container styling */
.table-container {
  display: block;
  width: 100%;
  max-width: 1200px; /* Fixed width for the main container */
  overflow-x: auto; /* Enables horizontal scroll on smaller screens */
  margin: 0 auto; /* Centers the container */
}

.expand-collapse-controls {
  display: flex;
  justify-content: space-between;
  padding: 8px;
}

/* Header row styling */
.header-row {
  display: grid;
  grid-template-columns: repeat(14, 150px); /* Adjust column widths */
  background-color: #f1f1f1;
  font-weight: bold;
  position: sticky;
  top: 0;
  z-index: 10;
}

/* Row styling */
.row {
  display: grid;
  grid-template-columns: repeat(14, 150px); /* Same widths as header */
  align-items: center;
  border-bottom: 1px solid #e0e0e0;
}

/* Cell styling */
.cell {
  padding: 8px;
  text-align: center;
  overflow: hidden;
  white-space: nowrap;
  text-overflow: ellipsis;
}

/* Scrollable content styling */
.content-rows {
  overflow-y: auto;
  max-height: 400px; /* Adjust as needed */
}
