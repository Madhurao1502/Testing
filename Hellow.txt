<mat-tab-group>
  <!-- Existing Tabs -->
  
  <!-- Create New Wave Tab -->
  <mat-tab label="Create New Wave">
    <form [formGroup]="multiweekForm">
      <div class="wave-container">
        <div class="grid-container">
          <!-- Left Grid: Select Weeks -->
          <div class="left-grid">
            <table mat-table [dataSource]="availableWeeks" class="mat-elevation-z8">
              <!-- Select Checkbox -->
              <ng-container matColumnDef="select">
                <th mat-header-cell *matHeaderCellDef></th>
                <td mat-cell *matCellDef="let week">
                  <mat-checkbox (change)="onWeekSelectionChange($event, week)" [checked]="isWeekSelected(week)">
                  </mat-checkbox>
                </td>
              </ng-container>

              <!-- MM -->
              <ng-container matColumnDef="MM">
                <th mat-header-cell *matHeaderCellDef>MM</th>
                <td mat-cell *matCellDef="let week">{{ week.monthYear }}</td>
              </ng-container>

              <!-- Week -->
              <ng-container matColumnDef="Week">
                <th mat-header-cell *matHeaderCellDef>Week</th>
                <td mat-cell *matCellDef="let week">{{ week.StartWeek }}</td>
              </ng-container>

              <!-- Start Date -->
              <ng-container matColumnDef="StartDate">
                <th mat-header-cell *matHeaderCellDef>Start Date</th>
                <td mat-cell *matCellDef="let week">{{ week.StartDate }}</td>
              </ng-container>

              <!-- End Date -->
              <ng-container matColumnDef="EndDate">
                <th mat-header-cell *matHeaderCellDef>End Date</th>
                <td mat-cell *matCellDef="let week">{{ week.endDate }}</td>
              </ng-container>

              <tr mat-header-row *matHeaderRowDef="leftGridColumns"></tr>
              <tr mat-row *matRowDef="let row; columns: leftGridColumns;"></tr>
            </table>
          </div>

          <!-- Right Grid: Wave Details -->
          <div class="right-grid">
            <table mat-table [dataSource]="waveData" class="mat-elevation-z8">
              <!-- Start Week -->
              <ng-container matColumnDef="StartWeek">
                <th mat-header-cell *matHeaderCellDef>Start Week</th>
                <td mat-cell *matCellDef="let wave">{{ wave.StartWeek }}</td>
              </ng-container>

              <!-- End Week -->
              <ng-container matColumnDef="EndWeek">
                <th mat-header-cell *matHeaderCellDef>End Week</th>
                <td mat-cell *matCellDef="let wave">{{ wave.EndWeek }}</td>
              </ng-container>

              <!-- Due Date -->
              <ng-container matColumnDef="DueDate">
                <th mat-header-cell *matHeaderCellDef>Due Date</th>
                <td mat-cell *matCellDef="let wave">
                  <input matInput [matDatepicker]="picker" formControlName="dueDate" (dateChange)="onDueDateChange(wave, $event)">
                  <mat-datepicker-toggle matSuffix [for]="picker"></mat-datepicker-toggle>
                  <mat-datepicker #picker></mat-datepicker>
                </td>
              </ng-container>

              <!-- Action -->
              <ng-container matColumnDef="Action">
                <th mat-header-cell *matHeaderCellDef>Action</th>
                <td mat-cell *matCellDef="let wave">
                  <button mat-button (click)="removeWave(wave)">Remove</button>
                </td>
              </ng-container>

              <tr mat-header-row *matHeaderRowDef="rightGridColumns"></tr>
              <tr mat-row *matRowDef="let row; columns: rightGridColumns;"></tr>
            </table>
          </div>
        </div>

        <button mat-raised-button color="primary" (click)="createWave()">Create Wave</button>
      </div>
    </form>
  </mat-tab>
</mat-tab-group>
