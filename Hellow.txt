.form-row {
  display: flex;
  align-items: center;
  margin-bottom: 18px;
}

.form-label {
  min-width: 160px;
  font-weight: 500;
}

.form-field {
  flex: 1;
  margin-left: 10px;
}

.select-width {
  min-width: 320px;
}


<div class="form-container">
  <div class="form-row">
    <label class="form-label" for="txtDelivery">Delivery</label>
    <mat-select class="form-field select-width" id="txtDelivery" [(value)]="delivery">
      <mat-option *ngFor="let del of deliveryList" [value]="del.deliveryName">
        {{ del.deliveryName }}
      </mat-option>
    </mat-select>
  </div>

  <div class="form-row">
    <label class="form-label" for="Project">Project</label>
    <input type="text" class="form-field" id="Project" [value]="projectName" readonly>
  </div>

  <div class="form-row">
    <label class="form-label" for="mdm">Key ID</label>
    <input type="text" class="form-field" id="mdm" [value]="keyId" readonly>
  </div>

  <div class="form-row">
    <label class="form-label" for="tsvversion">TSV Version</label>
    <mat-select class="form-field select-width" id="tsvversion" [(value)]="tsvVersion">
      <mat-option *ngFor="let tsv of tsvVersionNumber" [value]="tsv.tsvVersionID">
        {{ tsv.tsvVersionNumber }}
      </mat-option>
    </mat-select>
  </div>

  <div class="form-row">
    <label class="form-label" for="tsvcategory">TSV Category</label>
    <mat-select class="form-field select-width" id="tsvcategory" [(value)]="tsvCategory">
      <mat-option *ngFor="let cat of categoryList" [value]="cat">{{ cat }}</mat-option>
    </mat-select>
  </div>
</div>
