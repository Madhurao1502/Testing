/* Container for the entire table to make it scrollable on the x-axis */
.table-container {
  overflow-x: auto;
}

/* Table styling to ensure consistency */
.main-table {
  width: 1400px; /* You can adjust the fixed width based on the content */
  table-layout: fixed; /* Ensures columns are evenly distributed */
  border-collapse: collapse;
}

.main-table th, .main-table td {
  padding: 10px;
  border: 1px solid #ccc;
  text-align: left;
  white-space: nowrap; /* Prevent text from wrapping */
}

.main-table th {
  background-color: #f5f5f5;
  position: sticky;
  top: 0;
  z-index: 2;
}

/* Sticky Panel Header */
.mat-expansion-panel-header {
  background-color: #e0e0e0;
  border-bottom: 1px solid #ccc;
}

/* Scrollable container for the accordion content */
.scrollable-container {
  max-height: 600px; /* Adjust the height as needed */
  overflow-y: auto;
  border: 1px solid #ccc;
  padding: 10px;
}

/* Scrollable content within each accordion panel */
.scrollable-content {
  overflow-x: auto; /* Enable horizontal scrolling within the panel */
  overflow-y: auto;
  max-height: 300px; /* Adjust as needed */
}

/* Ensures the entire table layout works consistently when resized */
@media screen and (max-width: 1200px) {
  .main-table {
    width: auto;
  }
}
