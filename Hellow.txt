<mat-sidenav-container class="sidenav-container">
  <mat-sidenav #sidenav mode="side" opened>
    <div class="filter-section">
      <h3>Filters</h3>
      
      <!-- Date Filters -->
      <mat-form-field appearance="fill" class="filter-field">
        <mat-label>From Date</mat-label>
        <input matInput [matDatepicker]="fromDatePicker" />
        <mat-datepicker-toggle matSuffix [for]="fromDatePicker"></mat-datepicker-toggle>
        <mat-datepicker #fromDatePicker></mat-datepicker>
      </mat-form-field>

      <mat-form-field appearance="fill" class="filter-field">
        <mat-label>To Date</mat-label>
        <input matInput [matDatepicker]="toDatePicker" />
        <mat-datepicker-toggle matSuffix [for]="toDatePicker"></mat-datepicker-toggle>
        <mat-datepicker #toDatePicker></mat-datepicker>
      </mat-form-field>

      <!-- Mode Dropdown -->
      <mat-form-field appearance="fill" class="filter-field">
        <mat-label>Mode</mat-label>
        <mat-select [value]="selectedModes" multiple>
          <mat-option *ngFor="let mode of modes" [value]="mode.value">
            {{ mode.viewValue }}
          </mat-option>
        </mat-select>
      </mat-form-field>
    </div>
  </mat-sidenav>

  <mat-sidenav-content>
    <button mat-button (click)="sidenav.toggle()">Toggle Filters</button>

    <!-- Data Table -->
    <div class="data-section">
      <h3>Data</h3>
      <table mat-table [dataSource]="dataSource" class="mat-elevation-z8">
        <!-- Name Column -->
        <ng-container matColumnDef="name">
          <th mat-header-cell *matHeaderCellDef> P_Name </th>
          <td mat-cell *matCellDef="let element"> {{ element.name }} </td>
        </ng-container>

        <!-- Count Column -->
        <ng-container matColumnDef="count">
          <th mat-header-cell *matHeaderCellDef> P_Count </th>
          <td mat-cell *matCellDef="let element"> {{ element.count }} </td>
        </ng-container>

        <tr mat-header-row *matHeaderRowDef="displayedColumns"></tr>
        <tr mat-row *matRowDef="let row; columns: displayedColumns;"></tr>
      </table>
    </div>
  </mat-sidenav-content>
</mat-sidenav-container>
import { Component } from '@angular/core';

@Component({
  selector: 'app-filter-ui',
  templateUrl: './filter-ui.component.html',
  styleUrls: ['./filter-ui.component.css'],
})
export class FilterUiComponent {
  modes = [
    { value: 1, viewValue: 'First' },
    { value: 2, viewValue: 'Second' },
    { value: 3, viewValue: 'Third' },
  ];
  selectedModes = [1, 2, 3]; // Default selected modes

  displayedColumns: string[] = ['name', 'count'];
  dataSource = [
    { name: 'Product A', count: 10 },
    { name: 'Product B', count: 15 },
    { name: 'Product C', count: 7 },
  ];
}
