  <div>
          <!-- Left Grid -->
          <h3>Left Grid</h3>
          <table mat-table *ngIf="tableData.length>1" [dataSource]="leftGridData" class="mat-elevation-z8">
            <!-- Checkbox Column -->
            <ng-container matColumnDef="select">
              <th mat-header-cell *matHeaderCellDef>
                <mat-checkbox (change)="masterToggle()"
                              [checked]="isAllSelected()"
                              [indeterminate]="selection.hasValue() && !isAllSelected()">
                </mat-checkbox>
              </th>
              <td mat-cell *matCellDef="let row">
                <mat-checkbox (click)="$event.stopPropagation()"
                              (change)="$event ? selection.toggle(row) : null"
                              [checked]="selection.isSelected(row)">
                </mat-checkbox>
              </td>
            </ng-container>
        
            <!-- MM Column -->
            <ng-container matColumnDef="MM">
              <th mat-header-cell *matHeaderCellDef> MM </th>
              <td mat-cell *matCellDef="let element"> {{element.monthYear}} </td>
            </ng-container>
        
            <!-- Week Column -->
            <ng-container matColumnDef="Week">
              <th mat-header-cell *matHeaderCellDef> Week </th>
              <td mat-cell *matCellDef="let element"> {{element.startWeek}} </td>
            </ng-container>
        
            <!-- Start Date Column -->
            <ng-container matColumnDef="startDate">
              <th mat-header-cell *matHeaderCellDef> Start Date </th>
              <td mat-cell *matCellDef="let element"> {{element.startDate | date}} </td>
            </ng-container>
        
            <!-- End Date Column -->
            <ng-container matColumnDef="endDate">
              <th mat-header-cell *matHeaderCellDef> End Date </th>
              <td mat-cell *matCellDef="let element"> {{element.endDate | date}} </td>
            </ng-container>
        
            <tr mat-header-row *matHeaderRowDef="leftdisplayedColumns"></tr>
            <tr mat-row *matRowDef="let row; columns: leftdisplayedColumns;"></tr>
          </table>
          <button mat-raised-button color="primary" (click)="createWave()">Create Wave</button>
          <div *ngIf="errorMessage">{{ errorMessage }}</div>
        </div>
        
        <div>
          <!-- Right Grid -->
          <h3>Right Grid</h3>
          <table mat-table [dataSource]="rightGridData" class="mat-elevation-z8">
            <!-- Start Week Column -->
            <ng-container matColumnDef="startWeek">
              <th mat-header-cell *matHeaderCellDef> Start Week </th>
              <td mat-cell *matCellDef="let element"> {{element.startWeek}} </td>
            </ng-container>
        
            <!-- End Week Column -->
            <ng-container matColumnDef="endWeek">
              <th mat-header-cell *matHeaderCellDef> End Week </th>
              <td mat-cell *matCellDef="let element"> {{element.endWeek}} </td>
            </ng-container>
        
            <!-- Due Date Column -->
            <ng-container matColumnDef="dueDate">
              <th mat-header-cell *matHeaderCellDef> Due Date </th>
              <td mat-cell *matCellDef="let element">
                <input 
                [attr.min]="element.minDueDate" 
                [attr.max]="element.maxDueDate" 
                type="datetime-local" 
                [(ngModel)]="element.dueDate"
                (ngModelChange)="validateDueDate(element)">
            </td>
          </ng-container>
        
            <!-- Remove Button Column -->
            <ng-container matColumnDef="remove">
              <th mat-header-cell *matHeaderCellDef> Remove </th>
              <td mat-cell *matCellDef="let element">
                <button mat-button color="warn" (click)="removeWave(element)">Remove</button>
              </td>
            </ng-container>
        
            <tr mat-header-row *matHeaderRowDef="rightDisplayedColumns"></tr>
            <tr mat-row *matRowDef="let row; columns: rightDisplayedColumns;"></tr>
          </table>
        </div>
